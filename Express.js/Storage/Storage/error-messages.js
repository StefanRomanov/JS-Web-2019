const notAString = 'Only string keys are allowed'
const keyExists = "Key already exists"
const noKey = "Key doesn't exist"
const emptyStorage = "Storage is empty"

module.exports = {
    notAString,
    keyExists,
    noKey,
    emptyStorage
}